# Database Configuration
POSTGRES_USER=app_user
POSTGRES_PASSWORD=generate_a_secure_password_here
POSTGRES_DB=notifications

# Backend Configuration
# In docker-compose, 'db' is the hostname. Change if running outside docker.
DATABASE_URL=postgresql://app_user:generate_a_secure_password_here@db:5432/notifications
